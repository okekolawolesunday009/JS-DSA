//Write a function that takes an object 
//as a parameter and returns the number 
//of keys it has using the Object.keys() method. 


function countKeys(obj) {
	// Write your code below
	// const
	console.log(Object.keys(obj).length);
}
let person = {
	name: "kola"
};
countKeys(person);
