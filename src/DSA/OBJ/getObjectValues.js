//Write a function that takes an object 
//as a parameter and returns an array of 
//its values using the Object.values() method.

function getObjectValues(obj) {
	// Write your code below
    return Object.values(obj).map(objs => objs);

}


 
