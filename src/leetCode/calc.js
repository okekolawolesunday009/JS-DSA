/**
 * @param {string} s
 * @return {number}
 */
var calculate = function(s) {
	let token = s.match(/-?\d+(\.\d+)?|+\-*/()]/g)
//	for (let i = 0; i < token.length; i++) {
//		console.log(token[i]);
//	}
	console.log(token);

    
};
calculate("(1+2) + 2 + 3");
